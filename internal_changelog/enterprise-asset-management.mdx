---
title: Enterprise Asset Management
rss: true
---
<Update label="4 December 2025" description="1 Updates">

### 🐛 Bug Fixes

- **Vehicle Asset Creation (EAM-706)**
  - _Resolution_: Fixed a bug that could cause vehicle creation to fail if the default "Vehicle" asset type was missing for an organization. The system now automatically creates this default asset type if it doesn't exist, ensuring vehicles can always be added successfully.

---

</Update>

<Update label="3 December 2025" description="1 Updates">

### 🐛 Bug Fixes

- **Telematics Odometer Unit Conversion (EAM-703)**
  - _Resolution_: Fixed an issue where odometer data from telematics integrations was always stored in kilometers. The system now correctly recognizes the organization's unit of distance setting (e.g., miles) and converts the incoming data accordingly, ensuring accurate meter entries.

---

</Update>

<Update label="2 December 2025" description="3 Updates">

### 🔧 Manually Resolve Active Fault Codes

**What's New**  
You can now manually resolve active fault codes directly from an asset's details page. Additionally, a new indicator icon in the main assets list highlights which assets currently have active fault codes.

**Pre-requisites**
- Requires Admin access

<Accordion title="See More">

#### Where You'll See It
- *Enterprise Asset Management > Assets* (new warning icon in the assets table for any asset with active faults).
- *Enterprise Asset Management > Assets > [Select an Asset] > Fault Codes* tab (new "Mark as Resolved" option in the `...` actions menu for each active fault code).

#### What You Can Do
- Quickly identify assets that require attention directly from the main asset list.
- Manually resolve a fault code in the UI after the underlying issue has been fixed, without waiting for an update from an external integration.

#### Why This Matters
Previously, fault codes could only be resolved via automated integration updates. This feature empowers maintenance staff to update an asset's status in real-time, providing a more accurate and immediate view of fleet health.
</Accordion>

---
### 🔗 Re-authenticate Disconnected EAM Integrations

**What's New**  
A "Reauthenticate Integration" action is now available for EAM integrations with a "Disconnected" status, allowing you to easily re-establish the connection.

**Pre-requisites**
- Requires Admin access

<Accordion title="See More">

#### Where You'll See It
- *Enterprise Asset Management > Settings > Integrations* (a new "Reauthenticate Integration" option appears in the `...` actions menu for any integration with a `Disconnected` status).

#### What You Can Do
- If an integration disconnects (e.g., due to an expired token), you can now click "Reauthenticate Integration" to initiate the authentication flow again.

#### Why This Matters
Before, a disconnected integration could not be fixed from the UI, often requiring support intervention. This self-service feature enables admins to quickly restore broken connections, minimizing downtime for data synchronization.
</Accordion>

---
### 💡 EAM Lite Mode

**What's New**  
"EAM Lite" is a new, simplified mode for Enterprise Asset Management. When enabled via a feature flag, it streamlines the user interface by hiding more advanced features like Parts, Vendors, and Purchase Orders.

**Pre-requisites**
- Requires `EamLite` feature flag
- Requires Admin access

<Accordion title="See More">

#### Where You'll See It
- **Sidebar Navigation:** The *Enterprise Asset Management* sidebar is condensed to show only Assets, Issues, and Settings.
- **Asset Details:** When viewing an asset at *Enterprise Asset Management > Assets > [Select an Asset]*, the number of available tabs is reduced to a core set.
- **Superbox Search:** The global search bar will only return results for EAM Assets and Issues, hiding other EAM-related items.

#### What You Can Do
- Experience a cleaner, more focused EAM interface that prioritizes core asset and issue management workflows.

#### Why This Matters
This mode provides a less cluttered and more approachable experience for organizations that only need basic asset and issue tracking, making the EAM module easier to navigate and adopt.
</Accordion>

---

</Update>

<Update label="1 December 2025" description="2 Updates">

### 📎 Attach Documents and Images to Assets

**What's New**  
You can now upload and manage documents and images directly on an asset's profile. A new "Uploads" tab has been added to the asset view to store and display these files.

**Pre-requisites**
- Requires Admin access to the Enterprise Asset Management (EAM) product

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets > (Select an Asset) > Uploads tab*
- *EAM > Assets > New Asset* (new file input field)

#### What You Can Do
- Upload multiple files (e.g., registration documents, insurance papers, vehicle photos, warranty information) when creating or editing an asset.
- View, download, and remove uploaded files from the new "Uploads" tab on an asset's detail page.

#### Why This Matters
This centralizes all relevant documentation with the asset record, making it easy to find important files. It reduces the need to store critical documents in separate systems and improves record-keeping.
</Accordion>

---
### 🐛 Bug Fixes

- **Prevents Duplicate Fault Code Events (EAM-684)**
  - _Resolution_: Fixed an issue that could allow duplicate open fault codes to be created for the same asset. The system now prevents the creation of a new fault code event if an identical one is already open, ensuring data accuracy.

---

</Update>

<Update label="28 November 2025" description="3 Updates">

### ⚠️ Active Fault Code Indicator for Assets

**What's New**  
Assets with one or more active fault codes are now clearly marked with a warning icon throughout the EAM module. This provides an immediate, at-a-glance alert for assets that may require maintenance attention.

**Pre-requisites**
- Requires access to the Enterprise Asset Management (EAM) module.

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets* (a new warning icon appears in the asset list table).
- *EAM > Assets > [Asset Name]* (the same icon appears in the header of the asset detail page).

#### What You Can Do
- Quickly identify assets with active diagnostic trouble codes by looking for the yellow warning triangle icon.
- Hover over the icon to see a tooltip indicating the exact number of active fault codes for that asset.

#### Why This Matters
This change saves valuable time by surfacing critical maintenance alerts directly in primary asset views. It eliminates the need to manually check each asset's fault code tab, enabling fleet managers to respond more quickly to potential vehicle issues.
</Accordion>

---
### 🖨️ Print and Export Work Orders to PDF

**What's New**  
You can now generate a printable PDF summary of any work order with a single click. The generated document is professionally formatted for easy reading and sharing with external parties, such as maintenance vendors.

**Pre-requisites**
- Requires access to the Enterprise Asset Management (EAM) module.

<Accordion title="See More">

#### Where You'll See It
- *EAM > Work Orders > [Work Order #]* (a new "Print" button is available in the top-right corner of the page).

#### What You Can Do
- Click the "Print" button to open a new browser tab containing a paginated PDF of the work order.
- The PDF includes all key information: work order details, line items (parts and labor), cost breakdowns, and associated issues.

#### Why This Matters
This feature simplifies sharing work order details with third-party vendors or for internal record-keeping. It provides a standardized, professional format for all necessary maintenance information, reducing manual effort and improving communication.
</Accordion>

---
### 🧾 Comprehensive Asset Activity History

**What's New**  
The Activity tab for an asset now provides a complete historical log, tracking not only direct edits and comments but also key events from associated Work Orders and Issues.

**Pre-requisites**
- Requires access to the Enterprise Asset Management (EAM) module.

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets > [Asset Name] > Activity* tab.

#### What You Can Do
- View a chronological feed of all major lifecycle events for an asset in one place.
- See when a related Work Order was created, completed, or cancelled, and when a related Issue was created, resolved, or closed.
- Click on an event in the feed to navigate directly to the corresponding work order or issue.

#### Why This Matters
This creates a single source of truth for an asset's entire maintenance history. It eliminates the need to cross-reference multiple pages to build a timeline, making it much easier to track an asset's lifecycle and understand its full maintenance journey.
</Accordion>

---

</Update>

<Update label="27 November 2025" description="2 Updates">

### 🔳 Printable QR Code Generator for Parts

**What's New**  
You can now generate and print QR code labels for your inventory parts directly from the platform. This helps streamline inventory management and part check-out processes in the workshop.

**Pre-requisites**
- Requires Admin access to Enterprise Asset Management

<Accordion title="See More">

#### Where You'll See It
- *Enterprise Asset Management > Parts*

#### What You Can Do
- From the Parts list, click the new **"Generate Barcodes"** button to open the generator.
- Select a location and, optionally, specific parts for which to create labels.
- Customize the label dimensions (width and height in inches) to fit your label stock.
- Download a printable PDF. Each label includes the part name, its location in the stockroom (bin), a QR code, and a scannable UPC.

#### Why This Matters
Manually creating labels for inventory is time-consuming and error-prone. This feature automates the process, ensuring every part has a scannable, standardized label that links directly back to your EAM system, improving accuracy and efficiency.
</Accordion>

---
### 🐛 Bug Fixes

- **Default "Vehicle" Asset Type Creation (EAM-662)**
  - _Resolution_: Fixed an issue where the default "Vehicle" asset type was not automatically created for new organizations. This ensures that vehicle assets can be properly categorized from the start without manual intervention.

---

</Update>

<Update label="26 November 2025" description="3 Updates">

### 🖱️ Open EAM List Items in a New Tab

**What's New**  
You can now hold `Ctrl` (or `Cmd` on Mac) and click on rows in Enterprise Asset Management (EAM) lists to open the item's detail page in a new browser tab. This brings EAM lists in line with the behavior of other tables across the platform.

**Pre-requisites**
- Requires Admin access
- No feature flags required

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets* (table rows)
- *EAM > Issues* (table rows)
- *EAM > Parts* (table rows)
- *EAM > Service Reminders* (table rows)

#### What You Can Do
- Perform a normal click on any row to navigate to the detail page in the current tab.
- Hold `Ctrl` or `Cmd` and click a row to quickly open that item's detail page in a new tab for easy comparison or multitasking.

#### Why This Matters
Previously, all clicks on EAM list items would navigate in the same tab, forcing you to lose your place in the list. This update streamlines workflows by allowing you to open multiple items for reference without navigating away from the main list view.
</Accordion>

---
### ⛽️ View Asset-Specific Energy Entries

**What's New**  
A new "Energy Entries" tab is now available on the asset detail page. This tab appears automatically for any asset belonging to an asset type that has been configured to track energy, providing a centralized place to view its energy history.

**Pre-requisites**
- Requires Admin access
- No feature flags required

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets > (select an asset)* (new "Energy Entries" tab)
- *EAM > Energy Entries > Add Energy Entry* (form fields)

#### What You Can Do
- View a filtered list of all energy entries for a specific asset directly from its detail page.
- Click the "Add Energy Entry" button from the tab, which pre-fills the form with the current asset.
- Notice that the "Meter Entry" field on the energy entry form now only appears if the selected asset's type is configured for meter readings.

#### Why This Matters
Tracking energy consumption for a specific vehicle or asset was difficult, requiring you to search and filter the main Energy Entries list. This change provides a dedicated, filtered view on the asset's detail page, making it much easier to monitor an individual asset's energy usage.
</Accordion>

---
### 🐛 Bug Fixes

- **Improved Vehicle Asset Creation from Operations (EAM-660)**
  * _Resolution_: Fixed an issue where creating a vehicle in Operations without filling in all fields could result in an incomplete asset record in EAM. Now, missing required fields (e.g., make, model, license plate, year) are populated with safe default values ('-' or 0) to ensure a complete record is created.

- **Large Meter Entry Warning Restored (EAM-639)**
  * _Resolution_: Fixed a bug where the warning for creating a meter entry with an unusually large value (e.g., a typo like 150,000 instead of 15,000) was not triggering correctly. The validation logic has been fixed, and the warning now appears as expected.

---

</Update>

<Update label="25 November 2025" description="2 Updates">

### 🔢 Improved Meter Entry Sorting and Formatting

**What's New**  
On an asset's detail page, meter entries are now sorted with the highest (most recent) value at the top by default. Additionally, numeric values are formatted with thousand separators and the organization's distance unit (e.g., "120,000 mi") for better readability.

**Pre-requisites**
- Requires `EAM` feature flag
- Requires Admin access

<Accordion title="See More">

#### Where You'll See It
- *Enterprise Asset Management > Assets > (Select an Asset) > Meter Entries tab*

#### What You Can Do
- View the most recent meter reading at the top of the list without needing to sort the table.
- Read large meter values more easily thanks to improved number formatting.

#### Why This Matters
Previously, meter entries were sorted with the oldest, lowest values first. Displaying the most recent meter reading at the top makes it much faster for maintenance managers to see the current status of an asset.
</Accordion>

---
### 🐛 Bug Fixes

- **Work Order Filter Default (EAM-489)**
  - _Resolution_: Fixed an issue on the *Work Orders* page where the list did not correctly filter to "Assigned to Me" on initial page load, even though the tab was selected.

- **Payment Terminal Authentication (EAM-642)**
  - _Resolution_: Adjusted a security setting to resolve an issue that was preventing pop-up windows required for payment terminal authentication from opening correctly.

- **Issues Table Sorting (EAM-510)**
  - _Resolution_: Enabled sorting for the 'Asset' and 'Number' columns on the *Issues* page. Clicking these column headers will now correctly sort the list.

---

</Update>

<Update label="24 November 2025" description="3 Updates">

### ⛽ Manage and Sync Asset Fuel Levels

**What's New**  
You can now manually input, track, and filter assets by their fuel level. For organizations with telematics integrations, fuel levels can also be synced automatically from connected devices.

**Pre-requisites**
- Requires Admin access with permissions for Enterprise Asset Management

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets > (select an asset type with VIN/SN enabled) > Create/Edit Asset*: A new "Fuel Level (%)" input field is available.
- *EAM > Assets > Filters*: New "Fuel Level (Min)" and "Fuel Level (Max)" filters.
- *EAM > Assets > Columns*: A new "Fuel Level" column can be added to the asset list.
- *EAM > Assets > Asset Detail View*: The current fuel level is displayed.

#### What You Can Do
- Manually enter a fuel level percentage (0-100) when creating or editing an asset.
- Filter the asset list to find vehicles within a specific fuel level range.
- View the fuel level for assets in the list view and detail pages.

#### Why This Matters
This provides a more complete picture of an asset's status directly within Spare, helping with maintenance planning and operational readiness without needing to check external systems.
</Accordion>

---
### 🗓️ Clearer Wording for Future Out-of-Service Dates

**What's New**  
The information banner on an asset's detail page now uses clearer, context-aware language to indicate if an asset's out-of-service date is in the past or scheduled for the future.

**Pre-requisites**
- Requires Admin access with permissions for Enterprise Asset Management

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets > (select an asset) > Lifecycle tab*: The information banner at the top of the view.

#### What You Can Do
- When an out-of-service date is set for the future, the banner will now read: "This asset **will be** taken out of service on [Date]".
- For past or current dates, the banner remains: "This asset **has been** taken out of service on [Date]".

#### Why This Matters
Previously, the banner always used past-tense wording, which could be confusing for assets scheduled to be taken out of service in the future. This change improves clarity and reduces misinterpretation of an asset's active status.
</Accordion>

---
### 🐛 Bug Fixes

- **Asset Lifecycle Resale Value Accepts Zero (EAM-585)**
  - _Resolution_: Fixed an issue where the "Estimated Lifecycle Resale Value" field for an asset would not accept `0` as a valid input. The system now correctly handles a zero-dollar resale value, and related validation error messages now appear under the correct fields.
- **Inventory Adjustment Defaults to Current Quantity (EAM-544)**
  - _Resolution_: When using the "Adjust Inventory" action for a part, the "Actual Quantity" input field now defaults to the part's "Current Quantity" instead of `0`. This helps prevent accidental zeroing-out of inventory levels.

---

</Update>

<Update label="21 November 2025" description="2 Updates">

### 📝 Fault Code Descriptions

**What's New**  
When viewing fault codes for a vehicle, a new 'Description' column provides clear, human-readable explanations for each code, synced directly from your telematics provider.

**Pre-requisites**
- Requires Admin access to the EAM module.

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets > (select an asset) > Fault Codes tab* (new 'Description' column in the table).

#### What You Can Do
- Quickly understand the meaning of a fault code without needing to look it up externally.
- The description is displayed alongside the code, source, and timestamp for complete context.

#### Why This Matters
Fault codes were often cryptic and required external documentation to understand. By displaying the description directly in Spare, maintenance staff can diagnose issues faster and more efficiently.
</Accordion>

---
### 🐛 Bug Fixes

- **Precise Labor Time Entry (EAM-460)**
  - _Resolution_: When adding a labor line item to a work order, the 'Minutes' field now accepts any whole number instead of only increments of 60. This allows for accurate time logging (e.g., 75 or 90 minutes).

---

</Update>


<Update label="20 November 2025" description="2 Updates">

### ⚠️ View Live Asset Fault Codes

**What's New**  
For assets connected to a telematics integration, you can now view active diagnostic trouble codes (DTCs) directly within Spare. A new "Fault Codes" tab on the asset page lists all current fault codes reported by the vehicle.

**Pre-requisites**
- Requires the Enterprise Asset Management (EAM) product

<Accordion title="See More">

#### Where You'll See It
- *EAM > Assets > (select an asset) > Fault Codes tab*

#### What You Can Do
- Navigate to an asset's details page to see the new "Fault Codes" tab. This tab displays a table of current fault codes, including their description and the time they were detected.

#### Why This Matters
This feature provides maintenance teams with real-time visibility into vehicle health without needing to access a separate telematics portal. It allows for proactive maintenance by flagging issues as soon as they are detected by the vehicle's onboard diagnostics.
</Accordion>

---
### 🐛 Bug Fixes

- **Save Custom Fields on Work Order Creation (EAM-618)**
  - _Resolution_: Fixed an issue where custom field data was not being saved when creating a new work order. All custom field values entered during creation are now correctly stored and visible when viewing the work order.

---

</Update>


<Update label="19 November 2025" description="2 Updates">

### 🔧 Clearer Opt-Out for Smart Part Recommendations

**What's New**  
The setting to control smart part recommendations for a service task has been clarified. The field is now explicitly labeled "Opt out of smart part recommendations" and is only shown when creating or editing a service task, not in the main list view.

**Pre-requisites**
- Requires the `EAM` feature flag.
- Requires Admin access.

<Accordion title="See More">

#### Where You'll See It
- *EAM > Settings > Service Tasks > [New/Edit Service Task]*: The checkbox is now labeled **Opt out of smart part recommendations** with the hint text, "If yes, parts added to this task won't be suggested next time."
- *EAM > Settings > Service Tasks*: The column for this setting has been removed from the list view to reduce clutter.

#### What You Can Do
- Confidently enable or disable part suggestions for specific service tasks.
- Understand exactly what the setting does without ambiguity.

#### Why This Matters
The previous label for this setting was unclear. The new label and hint text make it obvious what the setting does, helping users manage part recommendations more effectively.
</Accordion>

---
### 🔗 New EAM Integrations Page for Telematics

**What's New**  
A new "Integrations" page has been added to EAM, allowing you to connect with external telematics providers. Meter entries created from these integrations will now clearly show the integration as their source.

**Pre-requisites**
- Requires the `eamIntegrations` feature flag.
- Requires Admin access.

<Accordion title="See More">

#### Where You'll See It
- *EAM > Settings > Integrations*: A new page for managing telematics and other EAM integrations.
- *EAM > Assets > [Select Asset] > Meter History*: The **Source** column will now display the name of the integration that created the meter entry.

#### What You Can Do
- View and manage connections to external telematics providers from a central location.
- Easily trace the origin of meter entries back to the specific integration that provided the data.

#### Why This Matters
This new page provides a central place to manage telematics integrations, making it easier to synchronize vehicle data like meter readings. Clearly sourcing meter entries improves data traceability and trust.
</Accordion>

---

</Update>


<Update label="18 November 2025" description="1 Updates">

### 🔧 "General" Tasks to Bypass Part Recommendations

**What's New**  
You can now mark a Service Task as "General." When a technician adds a general task to a Work Order, the system will not show historical part recommendations, streamlining the workflow for catch-all or miscellaneous tasks.

**Pre-requisites**
- Requires Admin access
- No feature flags required

<Accordion title="See More">

#### Where You'll See It
- *EAM > Service Tasks > [Edit a Task]* (a new "General" checkbox is available).
- *EAM > Work Orders > [Create/Edit a Work Order]* (when adding a line item, a hint appears for general tasks).

#### What You Can Do
- Edit a Service Task and check the **General** box to designate it as a catch-all task.
- When this task is added to a work order, technicians will see a hint: "No parts recommendations, this is for catch all tasks."

#### Why This Matters
Not all maintenance tasks have predictable part requirements. This feature allows you to create general service tasks (e.g., "General Inspection," "Miscellaneous Repair") without triggering unnecessary part recommendations, making the work order creation process faster.
</Accordion>

---

</Update>


<Update label="17 November 2025" description="3 Updates">

### 🔌 New Telematics Integration for Automated Meter Readings

**What's New**  
A new integration service has been introduced, allowing external telematics systems to connect to Spare's EAM. This enables the automatic creation of meter entries for assets, reducing manual data entry.

**Pre-requisites**
- No feature flags required.
- Requires Admin access with `eam:terminalConnection:create` permissions to configure new connections.

<Accordion title="See More">

#### Where You'll See It
- This is primarily a new backend capability. The results are visible in *EAM > Energy > Meter Entries*, where a new 'Telematics' source will appear for automatically created entries.

#### What You Can Do
- Work with Spare to connect external telematics systems to automatically sync asset meter readings into EAM.
- View meter entries created automatically from your telematics provider, identified by the 'Telematics' source type.

#### Why This Matters
Manually tracking and entering meter readings is time-consuming and prone to error. This integration automates the process, ensuring meter data in EAM is timely and accurate, which is crucial for scheduling preventive maintenance and maintaining compliance.
</Accordion>

---
### 📝 Add Notes and Track Task Completion in Work Orders

**What's New**  
You can now add general notes to a Work Order and specific notes to each line item. A new "Completed" column with a checkbox toggle allows technicians to mark individual service tasks as finished.

**Pre-requisites**
- No feature flags required.
- Requires Admin access with EAM permissions (e.g., `eam:workOrder:update`).

<Accordion title="See More">

#### Where You'll See It
- *EAM > Service > Work Orders > [Select a Work Order]*
- A new **Notes** field is available on the main work order view and in the create/edit forms.
- In the **Line Items** table, there is a new **Completed** column and an editable **Notes** field for each line item.

#### What You Can Do
- Add high-level notes to an entire work order for general context.
- Add specific notes to individual tasks for detailed instructions or observations.
- Click the circular checkbox in the **Completed** column to mark a task as done, which is saved automatically.

#### Why This Matters
Previously, there was no dedicated place for notes on work orders or their tasks, and no way to track the completion of individual line items. These changes provide better communication and progress tracking for maintenance work, ensuring all details are captured and task status is clear.
</Accordion>

---
### 🐛 Bug Fixes

- **Multi-Word Search in EAM (EAM-506)**
  - _Resolution_: Fixed an issue where searches with multiple words (e.g., "brake pad") in the EAM search bar were failing. The system now correctly handles spaces in search queries, returning accurate results.

---

</Update>

