## 2025-10-17 - Daily Update

---
title: "Internal Changelog"
description: "All internal product updates"
rss: true
---

<Update label="Friday, October 17, 2025" description="11 Updates" tags={["Operations", "Analytics", "AI Voice", "Admin", "Resolve"]}>

## 🎯 Operations
### ⏱️ Better Handling for Duty Interruptions

**What's New**  
When creating a duty interruption that is too long to fit before a duty's scheduled end time, the system will now automatically shorten it to the maximum possible duration. You will receive a specific notification indicating that the interruption has been shortened.

**Pre-requisites**
- Requires Administrator access

<Accordion title="See More">

#### Where You'll See It
- *Rides > Duties* (when clicking on a duty and using the 'Add Break' modal for an 'Interruption').

#### What You Can Do
- Create a duty interruption near the end of a duty's shift.
- If the interruption is too long, the system will create a shorter one and notify you of the adjusted duration (e.g., "Interruption created but shortened to 15 minutes").
- If no time is available at all, you will receive a clear failure message (e.g., "Could not create interruption as there is not enough time remaining in the duty").

#### Why This Matters
Previously, attempting to create an interruption that couldn't fit on a duty would simply fail without a clear reason. This change improves the workflow by creating a partial interruption when possible and providing clear, actionable feedback to dispatchers.
</Accordion>

---

## 📊 Analytics
### 📊 Query Group Membership Custom Fields Directly

**What's New**  
Custom fields on Group Memberships are now directly accessible as dimensions within the `Group Membership` data cube in Analytics, enabling more detailed reporting on your rider groups.

**Pre-requisites**
- Requires `AnalyticsV2` feature flag

<Accordion title="See More">

#### Where You'll See It
- *Analytics > Query Builder* (when selecting dimensions from the `Group Membership` data cube).

#### What You Can Do
- Build reports that filter, segment, or measure based on custom fields defined on your rider group memberships. For example, you can now report on trip counts by riders with a specific 'Certification Level' custom field.

#### Why This Matters
Previously, accessing group membership custom fields was limited and required complex queries. This change makes it much easier to build reports that directly leverage the unique data you store on group memberships.
</Accordion>

---
### 📊 Join Case Data with Administrator Details

**What's New**  
A new data relationship has been added in Analytics, allowing you to connect the `Case Data` cube to the `Organization Admin` cube to report on cases by their assigned administrator's details.

**Pre-requisites**
- Requires `AnalyticsV2` feature flag

<Accordion title="See More">

#### Where You'll See It
- *Analytics > Query Builder* (when building a report using the `Case Data` cube).

#### What You Can Do
- Create reports that include details about the administrator assigned to a case. For example, you can now group the total number of open cases by the assignee's first and last name.

#### Why This Matters
Previously, it was not possible to link case data to the administrators they were assigned to, limiting reporting capabilities. This change enables more detailed analysis of case management workloads by allowing you to include assignee information in your reports.
</Accordion>

---
### 🐛 Bug Fixes

- **Ridership Report Payment Method Capture (BUS-4303)**
  * _Resolution_: Corrected an issue where the payment method name (e.g., "Cash," "Ticket") was not being properly captured in the Analytics V2 Ridership Report.

---

## 🤖 AI Voice
### 🤖 More Accurate Date Recognition for Rider Authentication

**What's New**  
AI Voice now more rigorously validates date formats during rider authentication. It rejects ambiguous dates like "one one ninety two" (which could mean Jan 1 or Nov 1), while still correctly understanding clear formats like "June 6th 2025" and "2025 06 01".

**Pre-requisites**
- No feature flags required

<Accordion title="See More">

#### Where You'll See It
- This is a background improvement to the AI Voice rider authentication workflow when a caller provides their date of birth.

#### What You Can Do
- This is an automatic system improvement; no user action is needed. You can expect more reliable authentication outcomes.

#### Why This Matters
This change reduces the chance of authenticating the wrong rider due to ambiguous date understanding. It improves the security and reliability of the AI Voice authentication process by requiring clearer date inputs from callers.
</Accordion>

---

## ⚙️ Admin
### ✈️ New Cross-Region Data Migration Tool

**What's New**  
A completely redesigned Data Migration Tool (DMT) is now available for Super Admins. This new interface is specifically built to support migrating data for an organization from a source region (e.g., North America) to a destination region (e.g., Europe).

**Pre-requisites**
- Requires Super admin permissions

<Accordion title="See More">

#### Where You'll See It
- *Super Admin sidebar > Data Migrations*

#### What You Can Do
- Select a source and destination region from dropdown menus.
- Choose the source and destination organizations for the migration.
- Select specific data items to migrate (e.g., Services, Zones, Rider Groups).
- View real-time results of the migration process, with success or failure details for each item.

#### Why This Matters
This tool provides a robust, purpose-built solution for onboarding new organizations or restructuring existing ones across Spare's global infrastructure. It gives Super Admins granular control and clear feedback during complex cross-region data transfers.
</Accordion>

---
### 🗺️ New Organization-Level Multimodal Graph Configuration

**What's New**  
The configuration for multimodal graphs, which power public transit journey planning, has been redesigned and integrated directly into the organization details page. This new interface filters all datasets to the current organization, simplifying management and reducing errors.

**Pre-requisites**
- Requires Super admin permissions

<Accordion title="See More">

#### Where You'll See It
- *Super Admin > Organizations* (select an organization to see the new "Multimodal Configuration" section).

#### What You Can Do
- View the build status of the current graph and see when it was last updated.
- Trigger a graph rebuild directly from the UI using the "Request Rebuild" button.
- Manage all datasets (like GTFS feeds or flex routes) for the organization's graph in one place.
- When configuring a flex dataset, select from a list of services belonging only to that organization, preventing misconfiguration.

#### Why This Matters
The previous graph configuration UI was global, making it difficult to manage datasets for a specific organization. This new, organization-centric workflow is more intuitive, reduces the risk of errors, and provides clear visibility into the status of an organization's transit data graph.
</Accordion>

---
### ⚙️ Set a Default Country for Your Organization

**What's New**  
You can now set a "Default Country" for your organization. This country's flag and phone prefix will automatically appear in phone number input fields, such as when adding a new rider.

**Pre-requisites**
- Requires Administrator access to change the setting. Super Admins can set this when creating or editing an organization.

<Accordion title="See More">

#### Where You'll See It
- **Setting**: *Settings > Organization > General* (new "Default Country" dropdown field).
- **Effect**: *Riders > All Riders* (click "New Rider" to see the "Phone Number" field).

#### What You Can Do
- Navigate to your organization settings to select a default country.
- When creating new riders, the phone number field will default to your chosen country, simplifying data entry for staff.

#### Why This Matters
This helps reduce data entry errors and saves time for administrators by pre-filling the most common country code for your operations.
</Accordion>

---
### 🔒 Secure Photo URLs and Updated User Lists

**What's New**  
To enhance data security, the "Photo" column has been removed from the main list views for both Drivers and Administrators. Profile photos remain accessible on individual user profiles, where they are now served via secure, temporary URLs.

**Pre-requisites**
- No feature flags required

<Accordion title="See More">

#### Where You'll See It
- *Team > Drivers* (the "Photo" column is no longer present in the table).
- *Team > Administrators* (the "Photo" column is no longer present in the table).

#### What You Can Do
- This is an automatic security improvement. You can continue to view and manage user photos by navigating to their individual profile pages.

#### Why This Matters
This change aligns Driver and Administrator data handling with the security standards already in place for Riders. By removing permanent photo URLs from list views, it enhances user privacy and overall platform security.
</Accordion>

---
### 🐛 Bug Fixes

- **Improved Service Estimates Zone Validation (BUS-3786)**
  * _Resolution_: Fixed an issue where some services would not appear as available during the initial service estimate lookup. The system now validates location against service zones first, and only validates against time rules after a specific trip time has been selected.

---

## 🔧 Resolve
### 🐛 Bug Fixes

- **Form Address Field UI Cleanup (ENG-708)**
  * _Resolution_: Fixed an issue where headers from the address input field (e.g., "Search for an address") were incorrectly appearing in the table of contents on forms. The address search field has also been updated to align with the platform's standard look and feel.

</Update>

---

---

## 2025-10-17 - Daily Update

---
title: "Internal Changelog"
description: "All internal product updates"
rss: true
---

<Update label="Friday, October 17, 2025" description="11 Updates" tags={["Analytics", "AI Voice", "Admin", "Operations"]}>

## 📊 Analytics
### 📊 Query Group Membership Custom Fields Directly

**What's New**  
Custom fields associated with Group Memberships are now directly available for querying in Analytics. This allows for more detailed reports on group memberships without needing to join through the ridership report.

**Pre-requisites**
- Requires `AnalyticsV2` feature flag
- Requires Administrator access

<Accordion title="See More">

#### Where You'll See It
- *Analytics > Query Builder* (new dimensions available under the "Group Membership" data source)

#### What You Can Do
- Build reports that filter or group by custom fields on group memberships, such as `adaCode` or `disability`, directly from the `group_membership` data source.

#### Why This Matters
Previously, accessing these custom fields was limited and required complex joins through other data sources. Now, you can build more powerful and direct reports on your group members' attributes, simplifying the process of analyzing member data.
</Accordion>

---
### 🔗 Link Case Data with Administrator Details in Analytics

**What's New**  
You can now join `Case Data` with `Organization Admin` data in Analytics reports. This allows you to include details about the admin assigned to a case, such as their name or phone number, directly in your case reports.

**Pre-requisites**
- Requires `AnalyticsV2` feature flag
- Requires Administrator access

<Accordion title="See More">

#### Where You'll See It
- *Analytics > Query Builder* (when `Case Data` is selected as the data source, you can now add dimensions from the `Organization Admin` source)

#### What You Can Do
- Create a report on cases and include the first and last name of the `Assignee (Admin)`. For example, you can build a table showing the number of open cases assigned to each administrator.

#### Why This Matters
Previously, it was impossible to link case data to the administrators assigned to them, creating a reporting gap. This join enables more comprehensive analysis of case distribution and workload among your team.
</Accordion>

---
### 🐛 Bug Fixes

- **Ridership Report Payment Method Capture (BUS-4303)**
  - _Resolution_: Fixed an issue in the Ridership Report where payments made with "Cash/Ticket" were not correctly displaying the payment method type. The report now accurately reflects all payment methods used.

---

## 🤖 AI Voice
### 📅 More Accurate Date Recognition for Rider Authentication

**What's New**  
AI Voice now more rigorously validates date of birth formats provided by callers during authentication. It now rejects ambiguous dates while successfully understanding a wider range of valid formats.

**Pre-requisites**
- No feature flags required

<Accordion title="See More">

#### Where You'll See It
- This is a non-visual change that improves the behavior of the rider authentication process during an AI Voice call.

#### What You Can Do
- Callers can state their date of birth in various ways, like "June 6th 2025" or "2025 06 01," and be authenticated correctly. The system will now ask for clarification for ambiguous dates like "one one ninety two," which could mean January 1st or November 1st, 1992.

#### Why This Matters
This change improves the accuracy and security of rider authentication over the phone. It reduces the chance of authenticating the wrong rider due to an ambiguous date of birth while maintaining flexibility for callers.
</Accordion>

---

## ⚙️ Admin
### ✈️ New Cross-Region Data Migration Tool

**What's New**  
A new, dedicated user interface is now available for performing cross-region data migrations. This tool allows Super Admins to select source and destination regions and organizations, choose specific data items to migrate, and view detailed results of the migration process.

**Pre-requisites**
- Requires Super admin permissions

<Accordion title="See More">

#### Where You'll See It
- *System > Data Migrations* (a new page layout with dedicated source and destination selectors for region and organization)

#### What You Can Do
- Select a source region and organization.
- Select a destination region and organization.
- Choose specific data types (e.g., Riders, Services, Fare Rules) to migrate.
- Submit the migration and monitor the results in a dedicated panel showing success or failure for each item.

#### Why This Matters
This provides a robust, user-friendly tool for a complex administrative task. It reduces the risk of manual errors and provides clear visibility into the migration process when moving organizations between different geographic regions.
</Accordion>

---
### 🗺️ Redesigned Multimodal Graph Configuration

**What's New**  
The interface for configuring multimodal graphs has been completely redesigned and integrated directly into the organization's settings page. The new UI includes a graph status viewer, a "Request Rebuild" button, and a streamlined workflow for managing datasets that is scoped to the specific organization.

**Pre-requisites**
- Requires Super admin permissions

<Accordion title="See More">

#### Where You'll See It
- *System > Organizations > (Select an Organization) > View* (new "Multimodal Graph" section with "Status" and "Datasets" tabs)

#### What You Can Do
- Assign or create a multimodal graph directly from the organization's page.
- View the current build status of the graph (e.g., last build time, version).
- Click the "Request Rebuild" button to trigger a new graph build after data updates.
- Manage GTFS, real-time, and flex datasets in a simplified interface that only shows data relevant to the current organization.

#### Why This Matters
This overhaul simplifies a complex configuration process, reducing the risk of errors like assigning a dataset to the wrong graph or organization. The new status view and rebuild button give administrators direct visibility and control over their multimodal routing data.
</Accordion>

---
### 🌍 Set a Default Country for Your Organization

**What's New**  
Administrators can now set a default country for their organization. This country's flag and dialing code will automatically be pre-selected in phone number input fields, such as when creating a new rider.

**Pre-requisites**
- Requires Super admin permissions to set on organization creation.
- Requires Administrator access to set in organization settings.

<Accordion title="See More">

#### Where You'll See It
- *System > Organizations > (Create or Edit Organization)* (new "Default Country" dropdown field).
- *Settings > Organization > General* (new "Default Country" dropdown field).
- *People > Riders > All Riders > Create Rider* (the phone number input field will now default to the organization's selected country).

#### What You Can Do
- Set your organization's primary country (e.g., "United States"). When staff add new riders, the phone number field will default to the US flag and +1 dialing code, speeding up data entry.

#### Why This Matters
This simplifies creating new riders for organizations operating outside of the previous default. It ensures consistency and reduces the number of clicks needed for a common data entry task.
</Accordion>

---
### 🛡️ Enhanced Security for Admin and Driver Profile Photos

**What's New**  
Profile photos for administrators and drivers are now served via secure, time-limited URLs, matching the existing security for rider photos. As part of this change, the "Photo" column has been removed from the main list views for Drivers and Admins to improve performance and security.

**Pre-requisites**
- No feature flags required
- Requires Administrator access

<Accordion title="See More">

#### Where You'll See It
- *People > Drivers* (the "Photo" column is no longer in the table).
- *Settings > Team* (the "Photo" column is no longer in the table).
- Individual Driver and Admin profile pages will continue to show the photo, now loaded securely.

#### What You Can Do
- Continue to manage profile photos for drivers and admins on their individual profile pages. The underlying URLs for these images are now protected against unauthorized public access.

#### Why This Matters
This change ensures that all user profile photos across the platform are protected with the same high level of security, preventing direct, unauthenticated access to personal data.
</Accordion>

---
### 🐛 Bug Fixes

- **Group Membership Metadata Preservation (ENG-831)**
  - _Resolution_: Corrected an issue with the Group Memberships API endpoint where updating a single metadata field would incorrectly erase all other existing metadata. Partial updates now correctly merge changes, preserving all other data as expected.
- **Address Field Display in Forms (ENG-708)**
  - _Resolution_: Fixed a bug where headers from the address input field (e.g., "Search for an address") were incorrectly appearing in the table of contents on forms. The address input field has also been updated for better visual consistency.

---

## 🎯 Operations
### ⏱️ Smarter Scheduling for Duty Interruptions

**What's New**  
When creating a duty interruption, the system now intelligently handles situations where the full requested duration doesn't fit in the duty's remaining time. It will either automatically shorten the interruption to the maximum possible length or prevent it if no time is available, providing clear feedback in either case.

**Pre-requisites**
- No feature flags required
- Requires Administrator access

<Accordion title="See More">

#### Where You'll See It
- *Rides > Duties > (Select a Duty) > Breaks tab* (when clicking "Add Break" and creating an "Interruption").

#### What You Can Do
- If you try to add a 30-minute interruption to a duty that ends in 20 minutes, the system will now create a 20-minute interruption and notify you that it has been shortened. If no time is available, it will inform you that the interruption cannot be created.

#### Why This Matters
This prevents errors and confusion when scheduling breaks near the end of a duty. Instead of a generic failure, dispatchers get clear, actionable feedback and the system makes a logical adjustment, improving operational efficiency.
</Accordion>

---
### 🐛 Bug Fixes

- **Service Availability for Estimates (BUS-3786)**
  - _Resolution_: Fixed an issue where some services would not appear as available during the initial estimate phase due to strict time-based zone rules. The system now validates geography first, showing all geographically valid services, and applies time validation only after a specific trip time is selected.

</Update>

---

---

## 2025-10-17 - Daily Update

---
title: "Internal Changelog"
description: "All internal product updates"
rss: true
---

<Update label="Friday, October 17, 2025" description="10 Updates" tags={["Analytics", "AI Voice", "Admin", "Engine", "Operations"]}>

## 📊 Analytics
### 📊 Query Group Membership Custom Fields Directly

**What's New**  
Custom fields for Group Memberships are now directly available as dimensions in Analytics. You can build reports using these fields without needing to join through the Ridership Report data, simplifying your queries.

**Pre-requisites**
- Requires `AnalyticsV2` feature flag

<details>
  <summary><strong>See More</strong></summary>

  #### Where You'll See It
- *Analytics > Query Builder* (new dimensions available in the dimension picker for the `group_membership` data source)

  #### What You Can Do
- Add custom fields from Group Memberships directly as dimensions or filters when building a report.
- For example, if you have a custom field called "Certification Level" on your group memberships, you can now group or filter by that field directly.

  #### Why This Matters
Previously, accessing group membership custom fields required complex joins through the Ridership Report. This change makes reporting on this data much more straightforward and aligns it with how other custom fields work in Analytics.
</details>

---
### 🔗 Link Case Data to Organization Admins in Reports

**What's New**  
You can now join `Case Data` with `Organization Admin` data in Analytics reports. This allows you to include details about the admin assigned to a case, such as their name or email, directly in your case-related reports.

**Pre-requisites**
- Requires `AnalyticsV2` feature flag

<details>
  <summary><strong>See More</strong></summary>

  #### Where You'll See It
- *Analytics > Query Builder* (when using `Case Data` as the primary data source)

  #### What You Can Do
- When building a report on `Case Data`, you can now add dimensions from the `Organization Admin` data source (e.g., `Organization Admin > First Name`).
- Create reports that list all open cases and show the full name of the admin assigned to each one.

  #### Why This Matters
Previously, attempting to link case data to the assigned admin in a report would result in an error. This enhancement unlocks new reporting capabilities, allowing you to analyze case distribution and ownership more effectively.
</details>

---
### 🐛 Bug Fixes

- **Ridership Report Payment Method Capture (BUS-4303)**
  - _Resolution_: Fixed an issue where the `Ridership Report` in Analytics was not correctly showing the payment method for certain payment types (e.g., Cash, Ticket). All payment method types are now accurately reflected.

---

## 🤖 AI Voice
### 📅 More Accurate Date of Birth Recognition

**What's New**  
AI Voice is now stricter and more accurate when understanding dates of birth spoken by callers for authentication. It will now identify and reject ambiguous dates to prevent incorrect rider matches and improve security.

**Pre-requisites**
- No feature flags required

<details>
  <summary><strong>See More</strong></summary>

  #### Where You'll See It
- This is a behavioral change in the AI Voice system during rider authentication and has no specific UI location.

  #### What You Can Do
- Callers can authenticate using a variety of clear date formats.
- For example, AI Voice will correctly understand both "June 6th 2025" and "2025 06 01".
- However, it will now reject ambiguous spoken dates like "one one ninety two" (which could mean January 1st or November 1st) to avoid potential mismatches.

  #### Why This Matters
Previously, ambiguous dates could sometimes lead to incorrect authentications. This improvement ensures that only clearly stated dates of birth are accepted, increasing the accuracy and reliability of the voice authentication process.
</details>

---

## ⚙️ Admin
### 🌍 Set a Default Country for Your Organization

**What's New**  
You can now set a "Default Country" in your organization's settings. This country's flag and phone code will automatically be selected by default when adding phone numbers to new riders, streamlining the creation process for international operations.

**Pre-requisites**
- Requires Administrator access

<details>
  <summary><strong>See More</strong></summary>

  #### Where You'll See It
- *Settings > Organization > General Information* (new "Default Country" dropdown field)
- *People > Riders > Create Rider* (the phone number input field will now default to the selected country)

  #### What You Can Do
- Set a default country for your entire organization.
- When creating a new rider, the phone number field will pre-populate with the correct country code, reducing manual selection.

  #### Why This Matters
For organizations operating outside of North America, administrators had to manually select the correct country code for every new rider. This feature simplifies the workflow and reduces the chance of data entry errors.
</details>

---
### ✈️ Cross-Region Data Migration Tool

**What's New**  
A new Data Migration Tool is now available for Super Admins to transfer data between organizations in different geographic regions. The redesigned interface allows for selecting source and destination regions, choosing specific data types to migrate, and viewing detailed results of the migration process.

**Pre-requisites**
- Requires Super admin permissions

<details>
  <summary><strong>See More</strong></summary>

  #### Where You'll See It
- *Super Admin > Data Migrations* (new page at `/admin/super-admin/crossRegionDataMigrations`)

  #### What You Can Do
- Select a source and destination region from dropdown menus.
- Choose the specific source and destination organizations for the data transfer.
- Select which data items (e.g., Services, Riders, Fare Passes) to migrate.
- Monitor the results of the migration with per-item success and failure details.

  #### Why This Matters
This powerful internal tool provides a secure and reliable workflow for moving organization data between different hosting regions, which is critical for new customer onboarding and regional expansions.
</details>

---
### 🗺️ Redesigned Multimodal Graph Configuration

**What's New**  
The interface for managing multimodal journey planning graphs has been completely redesigned and integrated into each organization's settings. It now provides a streamlined, organization-specific view for managing datasets, checking graph build status, and requesting a rebuild with a single click.

**Pre-requisites**
- Requires Administrator access

<details>
  <summary><strong>See More</strong></summary>

  #### Where You'll See It
- *Settings > Organization > Multimodal Configuration* (new tab on the View Organization page)

  #### What You Can Do
- View the current build status of the journey planning graph, including the last build time.
- Click the "Request Rebuild" button to trigger a new graph build after updating data sources.
- Manage all GTFS, real-time, and on-demand flex datasets for the organization in one place.
- Assign datasets from a list that is automatically filtered to your organization, preventing misconfiguration.

  #### Why This Matters
Previously, configuring multimodal graphs was a complex, region-wide process prone to error. This new interface simplifies the entire workflow, reduces the risk of incorrect setup, and empowers administrators to manage their own journey planning data sources with confidence.
</details>

---
### 🐛 Bug Fixes

- **Driver and Admin Photo Display (BUS-4398)**
  - _Resolution_: For enhanced security, the `Photo` column has been removed from the main list views for *People > Drivers* and *Settings > Team*. Photos remain visible on individual driver and admin detail pages.
- **Address Field Headers in Form Viewer (ENG-708)**
  - _Resolution_: Fixed an issue where headers from the address input field (e.g., "Address Details") were incorrectly appearing in the table of contents when viewing forms. The address search input has also been visually updated for better consistency.
- **Group Membership Metadata Updates via API (ENG-831)**
  - _Resolution_: Resolved a bug in the `PATCH /v1/kcmSync/groupMemberships/:id` API endpoint where updating a single metadata field would incorrectly erase all other existing metadata. The endpoint now correctly merges the changes, preserving other fields.

---

## ⚙️ Engine
### ⏱️ Smarter Duty Interruption Scheduling

**What's New**  
When you create a duty interruption, the system now intelligently handles cases where the full duration won't fit before the duty ends. It will automatically create a shorter interruption if possible and notify you of the adjustment, preventing failures.

**Pre-requisites**
- Requires Administrator access

<details>
  <summary><strong>See More</strong></summary>

  #### Where You'll See It
- *Rides > Duties > (select a duty) > Driver Breaks modal*

  #### What You Can Do
- When creating a duty interruption (e.g., a break), the system checks for available time on the duty.
- If a 30-minute interruption is requested but only 20 minutes remain on the duty, the system will create a 20-minute interruption.
- A notification will appear confirming the change (e.g., "Interruption created but was shortened to fit on the duty.").

  #### Why This Matters
Previously, attempting to schedule an interruption that was longer than the remaining duty time would simply fail, forcing dispatchers to manually calculate the correct duration. This change automates the process, saving time and reducing frustration.
</details>

---

## 🎯 Operations
### 🐛 Bug Fixes

- **Service Availability for Estimates (BUS-3786)**
  - _Resolution_: Corrected an issue where a service might not appear as an option during trip estimation due to time-based rules on its service zones. The system now validates location first, showing all geographically valid services, and applies time-based rules only after a specific trip time is selected.

</Update>

---

---

